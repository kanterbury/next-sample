import { Meta, StoryObj } from "@storybook/react";
import { DelayInput } from ".";

export default {
  component: DelayInput,
} as Meta<typeof DelayInput>;

export const Primary: StoryObj<typeof DelayInput> = {};
